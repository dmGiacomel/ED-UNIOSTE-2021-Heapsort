int* allocateArray(int *array, long long int size);
void destructArray(int *array);

//Printa na saída padrão os elementos de um array entre colchetes com os elementos separados por vírgula
void printArray(int *array, long long int size);

// Ordenação através do método shellSort
void shellSort(int *a, int n);

// Lê valores do vetor pelo terminal
void readArray(int *array, int n);
